# Local-First CI-U1 Evidence

Date: 2026-08-24
Unit: `CI-U1`
Final commit: `d10278413c054a0f7233e8fe9ea5b0862dabce1e`
Final tree: `29652160b4bde1a6260b4bc9919851913ea8d2ae`

This file retains focused implementation and verification evidence only. Current-unit and next-action
authority remain in the active CI plan.

## Prototype reconciliation

- Inspected historical commits `4fe1cefb`, `e2b182df`, `375debe4`, `985b303e`, and `509d2a4a`
  file-by-file against the current branch.
- Rejected wholesale merge and cherry-pick because the prototype was 35 commits behind and encoded
  GitHub run identity in its core report schema.
- Retained the compatible tier, atomic-publication, failure-classification, and ignored-report ideas;
  replaced core identity with provider-neutral `testedSha`, `testedTree`, `executionId`, `attempt`,
  `trigger`, and `executorClass`. GitHub fields are optional adapter metadata only.

## Characterization

- Initial dirty primary-checkout cold fast run: 18,851 ms; it exposed unintended formatting of
  `.worktrees/` and led to explicit Prettier/ESLint exclusions.
- Dirty primary-checkout warm and cached probes: 5,159 ms and 5,203 ms; both correctly stopped at
  the pre-existing FS-U8 progress-document formatting change rather than reporting a false pass.
- Dirty primary-checkout post-commit preflight: 0 ms of gates; classified as infrastructure failure
  because exact integration evidence requires a clean checkout.
- Final clean-checkout fast duration: 63,134 ms. Final clean-checkout post-commit duration:
  103,687 ms. No service target was fixed from this initial sample.

## Final proof

Both final runs used the bundled Node runtime on a clean local clone, explicitly removed every
`GITHUB_*` variable from the environment, supplied no `GH_TOKEN`, and made no GitHub API, Actions
artifact, or remote-git request.

- `ci:fast`: passed all five gates. Report authority `developer-feedback`; `adapter: null`;
  workspace clean. SHA-256 of report:
  `7742b90a35eb96805bf8c10e9a3f3145d470559f3dce6b58740aaf3aa021b504`.
- `ci:post-commit`: passed all seven gates, including 34 API plus 2 database Worker test files and
  Admin/API/Storefront production builds. Report authority `integration`; `adapter: null`;
  workspace clean at admission. SHA-256 of report:
  `db2f4ab82bba33f9424f889688a7db31e9b5649d317bf62f6ae354eaf36537ef`.
- Both final JSON reports were copied to the primary checkout's ignored `artifacts/ci/` spool before
  temporary-clone cleanup.
- Focused contract proof: `bun test tools/ci-validate.test.ts` — 13 passed, 0 failed, 40 assertions.
- Focused formatting, ESLint, `tsconfig.tools.json` typecheck, and diff checks passed.

The production build intentionally rewrote the two tracked storefront active-generation files after
clean admission. This is retained as a runner-cleanup concern for CI-U3/CI-U4 and does not alter the
tested commit/tree or the CI-U1 report result.

## Temporary clone lifecycle

- Exact path: `/tmp/shoppp-ci-u1.xWwzZn/checkout`.
- Owner/purpose: Codex, clean exact-SHA CI-U1 fast/post-commit verification isolated from active
  FS-U8/IAM work.
- Tracked post-run changes: only
  `apps/storefront/app/generated/active-experience.ts` and
  `apps/storefront/app/generated/active-theme.ts`, generated by the production build.
- Material ignored paths: root/workspace `node_modules`, `artifacts/ci`, Admin/API build output,
  Storefront `.nuxt`, `.output`, generated collection/product output, and Worker output.
- Temporary-clone stash retained before the final SHA switch:
  `4797750ded4bfd8fbe4f95416f4ccc0ba8dba1c9`.
- Cleanup condition: final report identities, results, digests, tracked changes, ignored-path classes,
  and stash identity retained here. The initially proposed removal command was rejected by the
  safety policy; actual recoverable cleanup used `/usr/bin/trash /tmp/shoppp-ci-u1.xWwzZn` and
  verified that the original path no longer existed.
